// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000', // Replace this with your primary color
    },
    secondary: {
      main: '#E0E0E0', // Optional: for secondary elements
    },
  },
});

export default theme;
